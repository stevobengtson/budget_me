<div class="col-sm-2">
  <ul class="nav nav-pills nav-stacked">
    <% if logged_in? %>
    <%= render partial: 'accounts/list/list' %>
    <li class="<%= active_link?('category_groups') %>"><%= link_to "Category Groups", category_groups_url %></li>
    <li class="<%= active_link?('categories') %>"><%= link_to "Categories", categories_url %></li>
    <li><%= link_to 'Log out', logout_url, method: :delete, class: 'nav-link' %></li>
    <% else %>
    <li><%= link_to "Log in", login_url, class: 'nav-link' %></li>
    <% end %>
  </ul>
</div>
